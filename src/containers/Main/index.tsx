import React, { FC } from 'react';
import { Canon } from '../../components/Typography';

const Main: FC = () => {
    return (
        <Canon>
            Hello World
        </Canon>
    );
};

export default Main;